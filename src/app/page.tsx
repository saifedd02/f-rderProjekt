import FinderWorkspace from "@/components/workspace/FinderWorkspace";

/** Entry route. All interaction lives in the client-side workspace. */
export default function HomePage() {
  return <FinderWorkspace />;
}
